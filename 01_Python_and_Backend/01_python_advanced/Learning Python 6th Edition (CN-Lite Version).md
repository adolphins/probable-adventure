# Learning Python 6th Edition (CN-Lite Version)

经典 Python 进阶力作，Mark Lutz 著作 | O'Reilly 出品，适配 Python 3.12 最新版本，深入讲解面向对象编程核心精髓。


## Preface - 前言摘要


本版更新内容

- **移除 Python 2.X 相关内容**（已停止维护），专注于 Python 3.X（3.12 及更高版本）。
- 新增内容：f-字符串、命名赋值表达式（`:=`）、`match` 语句、类型提示、异步协程、星号解包扩展、下划线数字分隔符、`__main__.py`、`__getattr__` 模块钩子、`except*` 异常组、字典键插入顺序、仅位置函数参数、基于哈希的字节码文件等。
- 类型提示与异步协程仅简要介绍（因过于复杂，不适合初学者）。
- Unicode 内容更新，更强调 macOS、Android、Linux、iOS 等平台。
- 示例代码添加编号，减少冗余，精简篇幅。
- 两章高级内容移至线上（第38、39章）。
- 示例中的人名更中性（Bob → Pat），去除 Month Python 引用，减少第一人称叙述。


示例与更新

- 示例代码及勘误可在 [作者网站](https://learning-python.com/about-lp6e)、[示例包网站](https://learning-python.com/LP6E-code) 和 [出版社网站](https://www.oreilly.com/library/view/learning-python-6th/9781098171292/) 获取。


排版约定

- *斜体*：电子邮件、URL、文件路径、首次出现的术语。
- `等宽字体`：代码、文件内容、命令输出结果、模块/方法/语句/系统命令。
- **等宽粗体**：表示用户需要手动输入的命令或文本、代码高亮。
- *`等宽斜体`*：用于表示可替换内容（需自行填写的部分）。
- 提示框用于补充说明。


## Part I. Getting Started  入门

**Chapter 1. A Python Q&A Session - Python 问答环节**

主要回答初学者最常问的问题，帮助你理解 Python 是什么、为什么它如此流行。

**Chapter 2. How Python Runs Programs - Python 程序运行机制**

本章讲的是“Python 程序在底层是怎么跑起来的”，从技术层面解释 Python 代码的执行流程

- 解释器：Python 代码由解释器执行，不是直接运行在 CPU 上。

- 两步走：

    1. 编译 → 将源代码编译成字节码（保存在 __pycache__/*.pyc）→ 加速后续启动。
    2. 执行 → 字节码在Python虚拟机（PVM） 中解释执行。

**Chapter 3. How You Run Programs - 程序运行方式**

这一章是“动手操作指南”，告诉你怎么把写好的 Python 代码真正跑起来。


## Part II. Objects and Operations - 对象与操作符

### Chapter 4. Introducing Python Objects - 介绍 Python 对象

在 Python 中的一切皆为对象

- 既包括 Python 自带的内置对象（数据、运算、变量、函数都是对象）
- 也包括我们通过 Python 类或外部语言工具创建的自定义对象

> 对象本质上就是一段内存空间，包含数据值和对应的操作方法。

本章先快速概览 Python 的内置对象类型（数字、字符串、列表、字典等），为后续深入学习打基础。

#### The Python Conceptual Hierarchy - Python 概念层次结构

在开始编写代码前，我们先清晰梳理本章在整个Python知识体系中的位置。

从更具体的角度来看，Python程序可拆解为**模块、语句、表达式、对象**四层结构，关系如下：

1. 程序由模块组成
2. 模块包含语句
3. 语句包含表达式
4. 表达式创建并处理对象

本章节将从最底层开始——讲解内置对象，以及可用来操作这些对象的表达式。

**内置对象是学习 Python 的必经起点**。

---

**NOTE**

编程入门常强调三大核心：
- 顺序（先做这个，再做那个）
- 选择（条件成立则执行）
- 循环（重复执行）

Python也支持这三类逻辑，能帮你初步梳理思路。但这套划分过于简化且生硬，容易造成混淆。

例如：推导式同时包含循环和选择；这些术语在Python中还有更具体的含义；后续很多高阶概念也完全不符合这个框架。

在Python中，**更核心、更统一的原则是：对象，以及我们能对对象执行的操作**。

---

#### Why Use Built-in Objects? - 为什么使用内置对象？

在低级语言（如 C）中，你需要手动实现数据结构、管理内存、编写搜索访问代码等，这些工作繁琐易错，且分散精力。

在典型的Python程序中，这类繁重工作基本都消失了。
因为Python**内置了强大的对象类型**，你在开始解决问题之前，通常不需要自己编写对象实现代码。

事实上，除非你需要内置对象无法满足的特殊处理逻辑，否则几乎总是应该优先使用内置对象，而非自己实现。原因如下：

- **内置对象让程序更容易编写**
  对于简单任务，内置对象通常就足以表示问题域的结构。你可以直接使用列表、字典等强大工具，仅凭Python内置对象就能完成大量工作。

- **内置对象是扩展组件的基础**
  处理更复杂的任务时，你可能需要用类或C语言接口自定义对象。但这些自定义对象往往是构建在列表、字典等内置对象之上的。例如，栈结构可以用一个类来管理和定制内置列表实现。

- **内置对象通常比自定义数据结构更高效**
  Python内置对象使用的算法已经过深度优化，且很多是用C这类底层语言实现的，速度极快。你自己很难写出性能能与之匹敌的同类对象。

- **内置对象是语言的标准组成部分**
  你不需要为了开始编程而去实现专属对象类型。而且Python内置对象是统一标准，到处都一样；而私有工具库往往在不同环境中差异很大。

换句话说，内置对象不仅让编程更简单，而且比大多数从零实现的结构更强大、更易用。无论你是否实现新的对象类型，**内置对象都是所有Python程序的核心**。

#### Python's Core Object Types - Python 的核心对象类型

Python 内置了多种核心对象类型，每种都有专用的字面量语法，创建后其操作集即固定（动态但强类型）

**表 4-1 Python 内置（核心）对象**

| 类型     | 字面量/创建示例                                  | 简要说明                                     |
| -------- | ------------------------------------------------ | -------------------------------------------- |
| Numbers - 数字     | `123`, `3.14`, `3+4j`, `Decimal('1.0')`          | 整数、浮点、复数、小数、分数等               |
| Strings - 字符串   | `'code'`, `"app's"`, `b'a\x01c'`, `'h\u00c4ck'`  | 文本（Unicode）或字节序列，不可变            |
| Lists - 列表     | `[1, [2, 'three'], 4.5]`, `list(range(10))`      | 有序、可变、任意嵌套的集合                   |
| Dictionaries - 字典     | `{'job': 'dev', 'years': 40}`, `dict(hours=10)`  | 键值映射，保留插入顺序，可变                 |
| Tuples - 元组     | `(1, 'app', 4)`, `tuple('hack')`                 | 不可变序列，常用于固定集合                   |
| Files - 文件     | `open('data.txt')`, `open(r'C:\data.bin', 'wb')` | 访问外部文件，通过 `open` 创建               |
| Sets - 集合     | `{'a', 'b', 'c'}`, `set('abc')`                  | 无序、唯一元素，支持数学运算                 |
| Other core objects - 其他核心 | `True`, `False`, `None`, `type`                  | 布尔值、空占位符、类型对象                   |
| Program-unit objects - 程序单元 | 函数、模块、类                                   | 通过 `def`、`import`、`class` 创建，也是对象 |
| Implementation objects - 实现相关 | 编译后的代码、栈回溯                             | 通常供工具开发者使用                         |

同样重要的是，对象一旦创建，其支持的操作就固定不变 —— 字符串只能执行字符串操作，列表只能执行列表操作。

正式来讲，这意味着 Python 是动态类型语言，会自动跟踪对象类型，无需编写类型声明代码；同时它也是强类型语言，限制对象只能执行自身类型支持的操作。


#### Numbers - 数字

Python 的核心对象类型里包含最常见的类型：

- 没有小数部分的整数
- 带小数部分的浮点数
- 还有更特殊的类型
    - 带虚部的复数
    - 可变精度的十进制数
    - 带分子分母的分数

内置数字足以表示绝大多数数值，从你的年龄到银行余额，向量、矩阵这类专用数值对象也可以通过第三方库获得。

虽然 Python 提供了更高级的选项，但基本数字对象其实很基础。

Python 的数字支持常规数学运算。比如，加号 + 执行加法，星号 * 表示乘法，两个星号 ** 表示乘方


```python
$ python3           # 启动REPL
>>> 123 + 222       # 整数加法
345
>>> 1.5 * 4         # 浮点数乘法
6.0
>>> 1_234_567, 0x15, bin(21)  # 分隔符、十六进制、二进制
(1234567, 21, '0b10101')
>>> 2 ** 100        # 2的100次方
1267650600228229401496703205376
```

注意最后一个结果：Python 的整数对象会根据需要自动提供超大精度。

你甚至可以在 Python 里计算 2 的 12345 次方，不过结果太长 —— 有近 4000 位，不太容易看懂：

```python
>>> len(str(2 ** 12345))  # 计算多少位
3717
```

这种嵌套调用是从内向外执行的 — 先算出乘方结果，再用内置函数str把它转成数字字符串，最后用len获取字符串长度。

除了表达式，Python 还有一些实用的数值模块，它们只是一些附加工具包，我们需要用import语句导入才能使用：

```python
>>> import math
>>> math.pi
3.141592653589793
>>> math.sqrt(85)
9.219544457292887
```

math模块包含更多高级数值函数，而random模块可以生成随机数和随机选择

```python 
>>> import random
>>> random.random()
0.7082048489415967
>>> random.choice([1, 2, 3, 4])
2
```

Python 还包含更特殊的数值对象 —— 比如复数、定点数、有理数，以及集合和布尔型 —— 第三方开源扩展库则提供更多类型（如矩阵、向量、更高精度数字）。这些对象我们会在本章和本书后面再讲。

到目前为止，我们一直在把 Python 当成简单的计算器使用；为了更全面地介绍它的内置对象，我们接下来继续讲解字符串。

####  Strings 字符串

字符串用于记录文本信息（比如你的名字），以及任意的字节集合（比如图像文件的内容）。

它们是 Python 中我们第一个接触到的序列 —— **按位置顺序排列的对象集合**。序列会保持元素从左到右的顺序：其中的元素按相对位置存储和读取。

严格来说，字符串是单字符的序列；其他更通用的序列对象包括后面会讲到的列表和元组。


##### Sequence Operations

作为序列，字符串支持假定元素按位置排序的操作。

例如，如果我们有一个用引号括起来的 4 字符字符串（单引号和双引号含义相同，单引号更常用、更简洁），我们可以用内置函数len验证它的长度，并用索引表达式获取其中的元素：

```python 
>>> S = 'Code'  # 创建一个4字符字符串
>>> len(S)     # 长度：字符个数
4
>>> S[0]       # S中的第一个元素，索引为0
'C'
>>> S[1]       # 从开头数第二个元素
'o'
```

在 Python 中，索引写在方括号里，表示从开头算起的偏移量，所以从 0 开始：第一个元素在索引 0，第二个在索引 1，依此类推。



##### Immutability





##### Type-Specific Methods





##### Getting Help





##### Other Ways to Code Strings





##### Unicode Strings

